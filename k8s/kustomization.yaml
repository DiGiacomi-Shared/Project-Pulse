apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: project-pulse

resources:
  - namespace.yaml
  - api-deployment.yaml
  - frontend.yaml
  - ingress.yaml

images:
  - name: project-pulse-api
    newTag: latest
  - name: project-pulse-frontend
    newTag: latest
